#include <stdio.h>

int	main()
{
	float	num = 5040;
	float	divideby = 362880;
	float	result = (num / divideby);
	printf("%f / %f = %f\n", num, divideby, result);
	float	num2 = 40320;
	float	multiplyby = 45;
	float	result2 = (num2 * multiplyby);
	printf("%f * %f = %f", num2, multiplyby, result2);
}
